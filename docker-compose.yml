services:
  gamesync:
    build: .
    container_name: gamesync
    ports:
      - "8080:8080"
    environment:
      GAMESYNC_LOGGING: true
    volumes:
      - ./data/sqlite_db:/srv/gamesync/sqlite_db
