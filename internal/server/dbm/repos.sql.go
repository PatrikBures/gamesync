// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: repos.sql

package dbm

import (
	"context"
)

const createRepo = `-- name: CreateRepo :one
INSERT INTO repos (user_id, repo_name)
VALUES ($1, $2)
RETURNING repo_id
`

type CreateRepoParams struct {
	UserID   int64
	RepoName string
}

func (q *Queries) CreateRepo(ctx context.Context, arg CreateRepoParams) (int64, error) {
	row := q.db.QueryRow(ctx, createRepo, arg.UserID, arg.RepoName)
	var repo_id int64
	err := row.Scan(&repo_id)
	return repo_id, err
}

const listRepos = `-- name: ListRepos :many
SELECT repo_id, user_id, repo_name FROM repos
WHERE user_id = $1
`

func (q *Queries) ListRepos(ctx context.Context, userID int64) ([]Repo, error) {
	rows, err := q.db.Query(ctx, listRepos, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Repo
	for rows.Next() {
		var i Repo
		if err := rows.Scan(&i.RepoID, &i.UserID, &i.RepoName); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
