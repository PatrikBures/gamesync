test-client-server:
  stage: test
  image: $CLIENT_TEST_IMAGE:latest
  extends:
    - .golang-before-merge
    - .gocache
  needs: 
    - job: build-test-client-container
  services:
    - name: ${CI_REGISTRY_IMAGE}:${VERSION}
      alias: server
  script:
    - go mod tidy
    - go test -v ./...
